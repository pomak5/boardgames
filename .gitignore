node_modules/
dist/
.env*
!.env.example
*.log
.DS_Store

# build artifacts
*.tsbuildinfo
.tsbuild/

# working screenshots (root + apps/space)
/*.png
apps/space/*.png

# stray npm lockfile (project uses bun)
package-lock.json

# локальный dev-override docker compose (порт-конфликты и т.п. — machine-specific)
docker-compose.override.yml
.superpowers/
